# Xposed API JAR
# 
# This directory should contain api-82.jar
# Download it before building:
#
# Windows:
#   Invoke-WebRequest -Uri "https://api.xposed.info/downloads/XposedBridgeApi-82.jar" -OutFile "app/libs/api-82.jar"
#
# Linux/macOS:
#   curl -L -o app/libs/api-82.jar https://api.xposed.info/downloads/XposedBridgeApi-82.jar
#
# GitHub Actions will download it automatically.
